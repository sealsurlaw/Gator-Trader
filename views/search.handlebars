
{{#if data}}
<!-- Navigation Bar w/ Dropdowns -->
<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>
    <div class="collapse navbar-collapse" id="myNavbar">
      <ul class="nav navbar-nav">
        <a href="#" style="text-decoration:none;"><h5 style="color:#fff; font-size:150%;">Showing {{data.length}} of {{data.length}} results</h5></a>
      </ul>

      <ul class="nav navbar-nav navbar-right">

        {{!-- <div class="dropdown">
          <button class="btn btn-primary dropdown-toggle" type="button" data-toggle="dropdown">Filter
          <span class="caret"></span></button>
        </div> --}}

      </ul>
    </div>
  </div>
</nav>

{{/if}}


  <!-- Left-aligned media object -->
  {{#if data}}
  {{#each data}}
  <div class="media">
    <div class="media-left">
    <a href="/item/{{this.item_id}}" target="_blank"><img src="{{this.item_image_thumbnail}}" class="media-object" style="width:200px"></a>
    </div>
    <div class="media-body">
      <h3 class="media-heading">Item Name: {{this.item_title}}</h3>
      {{!-- <h4>Category: </h4> --}}
      <h4>Price: {{this.item_price}}</h4>
      <br>
      <br>
      <br>
      <button type="button" class="btn btn-info" onclick="window.open('/item/{{this.item_id}}', '_blank')">View Details</button>
      <button type="button" class="btn btn-warning"onclick="window.open('/item/{{this.item_id}}?open=true', '_blank')" >Contact Seller</button>
    </div>
  </div>

  <!--Modal-->
<div class="modal fade" id="Message" tabindex="-1" role="dialog" aria-labelledby="Message" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h4 class="modal-title" id="Message">Contact Seller</h4>
      </div>
      <div class="modal-body">
           <form>
          <div class="form-group">
            <label for="contactEmail" class="col-form-label">Email:</label>
            <input type="email" class="form-control" id="contactEmail">
          </div>
          <div class="form-group">
            <label for="message-text" class="col-form-label">Message:</label>
            <textarea class="form-control" id="message-text" placeholder="I am interested in your {{this.item_title}} for sale"></textarea>
          </div>
        </form>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
        <button type="button" class="btn btn-primary">Send Message</button>
      </div>
    </div>
  </div>
</div>

  <hr>
  {{/each}}
  {{/if}}
